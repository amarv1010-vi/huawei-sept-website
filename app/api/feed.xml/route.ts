import {articles} from '@/lib/intelligence';const e=(s:string)=>s.replace(/[<>&]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;'}[c]!));export function GET(){const b='https://huawei-intelligence.amarvi.chatgpt.site';const x='<?xml version="1.0"?><rss version="2.0"><channel><title>Huawei Intelligence</title><link>'+b+'</link>'+articles.map(a=>`<item><guid>${a.id}</guid><title>${e(a.title)}</title><link>${b}/news/${a.id}</link><description>${e(a.summary)}</description><pubDate>${new Date(a.date+'T12:00:00Z').toUTCString()}</pubDate></item>`).join('')+'</channel></rss>';return new Response(x,{headers:{'Content-Type':'application/rss+xml'}})}
