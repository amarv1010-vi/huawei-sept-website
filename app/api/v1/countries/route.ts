import {countries} from '@/lib/intelligence';export function GET(req:Request){const p=new URL(req.url).searchParams;const a=countries.filter(c=>(!p.get('region')||c.region===p.get('region'))&&(!p.get('level')||c.level===p.get('level')));return Response.json({items:a,total:a.length,asOf:'2026-09-12',assessment:'Analyst judgment; not market share'},{headers:{'Access-Control-Allow-Origin':'*'}})}
