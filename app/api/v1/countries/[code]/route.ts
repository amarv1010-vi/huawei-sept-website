import {countries,articles} from '@/lib/intelligence';export async function GET(_:Request,{params}:{params:Promise<{code:string}>}){const p=await params;const c=countries.find(x=>x.code===(p.code||'').toUpperCase());return c?Response.json({...c,records:articles.filter(a=>a.countries.includes(c.code))}):Response.json({error:'Unknown country'},{status:404})}
