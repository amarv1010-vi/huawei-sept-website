import {articles,current,search,countries} from '@/lib/intelligence';export function GET(req:Request){const p=new URL(req.url).searchParams;const a=search(p.get('q')||'').filter(x=>(!p.get('country')||x.countries.includes(p.get('country')!.toUpperCase()))&&(!p.get('region')||x.countries.some(code=>countries.find(c=>c.code===code)?.region===p.get('region')))&&(!p.get('technology')||x.technology===p.get('technology'))&&(!p.get('current')||current(x)===(p.get('current')==='true')));return Response.json({items:a,total:a.length,asOf:'2026-09-12'},{headers:{'Access-Control-Allow-Origin':'*'}})}
